# Release 2.1.2 — out-of-scope health signal + diagnostic doc clarifications

**Date:** 2026-06-27

## Goal
Cut 2.1.2 bundling the out-of-scope orphan health signal and two operator-facing
doc clarifications.

## Constraints
- Drop-in from 2.1.1; no schema/data/scope-semantics change.

## Touched Surfaces
- Version bump: `pyproject.toml`, `forward_netbox/__init__.py`, the 3 README
  tables, `CHANGELOG.md`.
- Feature: out-of-scope orphan health signal — see
  `2026-06-27-out-of-scope-health-signal.md`.
- Docs: parent-prefix diagnostic wording (`2026-06-27-tighten-parent-prefix-warning.md`)
  and the backfilled-vs-out-of-scope Operations Guide note.

## Approach
Standard release: prepare bumps version + README + CHANGELOG; verify (suite, lint,
harness, sensitive, build); publish FF main + tag + GitHub release + PyPI.

## Validation
Full suite (902). Lint/harness/sensitive. PR CI green before merge; main CI green
after. No `.nqe` change this release, so no CustomerOrg org query republish needed.

## Rollback
Revert the release commit; `pip install forward-netbox==2.1.1`.

## Decision Log
- Bundled the feature with the two staged doc clarifications rather than a docs-
  only point release, since the health signal is the user-facing reason to upgrade.
