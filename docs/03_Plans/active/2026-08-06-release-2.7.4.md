# Release 2.7.4

## Goal

Ship the tranche that came out of one day of customer reports: a sync that died
on a single interface, six issue rows that named nothing, and three ingestions
that would not delete. Plus netbox-dlm `0.7.0`, which the customer released and
runs on the same NetBox.

## Contract

- The four version surfaces move in ONE commit. Splitting them reverts the fast
  baseline from minutes to hours with no error.
- The release commit carries its own release-authorization section, and the
  evidence base commit recorded there must equal the parent of the commit that
  gets tagged. The publish workflow checks this against the tagged tree, so a
  tag pushed without it is unpublishable and unrecoverable. Never write that
  section's heading text anywhere earlier in the plan: the checker splits on the
  first occurrence of the literal heading, so a prose mention of it upstream
  makes the checker read the wrong section and report the evidence as missing.
- The tag is created locally, provenance verified, and only then pushed.
- The first first-parent commit after the tag is documentation-only. That slot
  cannot be reclaimed.
- No `.devN` is opened on `main` afterwards.

## Constraints

- The prior-release tag is hardcoded in SIX coupled places: `PRIOR_RELEASE_TAG`
  and `PRIOR_POST_RELEASE_DOC_COMMIT` in `verify_release_provenance.py`, the tag
  fetch in `release.yml`, two assertions in `check_harness.py`, a workflow
  fixture in `scripts/tests/test_check_harness.py`, and one in
  `scripts/tests/test_tasks.py`.
- `check_harness.py` reads the real workflow, so it passes while the two
  fixture-based tests fail. Only `python3 -m unittest discover -s scripts/tests`
  catches that pair.
- The plan-file gate is per COMMIT, not per PR.
- `release.py <version> --write` inserts the candidate row itself, and
  `stage_prepare` then refuses with "a release candidate already exists". Reset
  the eight staged files and run the prepare that carries the summary instead of
  trying to patch the empty row.
- No customer identifiers in the release notes: the interface failure is
  described by rule, never by device.

## Touched Surfaces

- `pyproject.toml`, `forward_netbox/__init__.py`,
  `forward_netbox/utilities/fast_baseline.py`,
  `forward_netbox/tests/test_runtime_dependency_check.py`
- `README.md`, `docs/README.md`, `docs/01_User_Guide/README.md`, `CHANGELOG.md`

## Approach

Everything in this release is already on `main` and already ran the full plugin
suite: #144, #145, #146, #147, #148, #149, #150. This change is the version bump
and the release notes; the gate then re-runs the lot against the built artifact.

## Validation

`invoke ci` and `invoke artifact-test` on the pinned environment, then the tag,
then `verify_release_provenance.py` against the local tag before pushing it.

## Rollback

Before the tag is pushed, revert the branch. After it is pushed, the tag is
immutable and the ruleset forbids moving or deleting it — a defect found then
costs a version number, which is why the upgrade gate runs in PR CI rather than
at tag time.

## Decision Log

- **Cut now rather than accumulate.** The customer installs from `main` between
  releases, and `main` declares the released version, so his NetBox reported
  `2.7.2` while running four commits past it. Which commits he had was
  determinable only by correlating his ingestion timestamps against merge times.
- **`#46` and `#54` deliberately excluded.** Device-identity pruning touches
  ownership release, where pruning wrongly drops a claim on a live device;
  scoping the CI trigger changes CI behaviour for the very PR that carries it.
  Neither belongs in a release cut under time pressure.
- **`2.7.3` was burned, and this is the same tree renumbered.** `v2.7.3` was
  tagged on a release commit whose plan carried no release-authorization
  section. `check_release_authorization.py` runs against the tagged tree and
  refused, so the publish job stopped at its first gate and nothing was built or
  uploaded. The tag ruleset carries `deletion` and `non_fast_forward` on
  `refs/tags/v*`, so the tag can be neither moved nor removed, and weakening the
  ruleset to reclaim a version number is not a trade worth making. The cost was
  one version number and one CI cycle.
- **The gates ran before the authorization prose existed.** That ordering is
  inherent: the evidence describes gates that must already have passed, so the
  section can only be written afterwards, and writing it changes the tree the
  gates ran on by exactly that prose. `2.7.2` shipped the same way, as a
  `prepare` commit and an `authorize` commit squashed into one. What the checker
  actually binds is the evidence base commit against the tagged commit's parent,
  which is what stops authorization being transplanted onto a different tree.

## Open

- `main` will declare `2.7.4` while accumulating post-release commits again.
  That is the accepted cost of not opening a `.devN`; the next release must bump
  the surfaces as its first post-tag act.
- Two of the three ingestions the customer cannot delete are fixed here. Any
  held by device identities for departed devices are not (#46).

## Release Authorization

Both gates ran against the final tree, whose parent is
`bdda04cbd79d8edad013d9e64333e815a2d7a83b`, on a clean working tree.

- Evidence base commit: `bdda04cbd79d8edad013d9e64333e815a2d7a83b`
- [x] `final-tree-full-gate` - `rtk env FORWARD_NETBOX_DOCKER_PROJECT=forward-netbox-release-gate FORWARD_NETBOX_POSTGRES_DATA_PATH=netbox-postgres-data FORWARD_NETBOX_WORKER_AUTORELOAD=0 NETBOX_VER=v4.6.6 FORWARD_NETBOX_HOST_PORT=8123 NETBOX_URL=http://127.0.0.1:8123 invoke ci` passed with status 0 across the whole fan-out: 328 harness tests, 70 scenario tests, 1969 plugin tests and the 1 UI harness test all reported OK, the strict sync release gate passed, and the 5 artifact-upgrade paths each seeded under a previous release, migrated onto the built wheel, and kept the rows seeded under that previous release.
- [x] `exact-runtime-artifact` - `rtk env FORWARD_NETBOX_DOCKER_PROJECT=forward-netbox-release-gate FORWARD_NETBOX_POSTGRES_DATA_PATH=netbox-postgres-data FORWARD_NETBOX_WORKER_AUTORELOAD=0 NETBOX_VER=v4.6.6 FORWARD_NETBOX_HOST_PORT=8123 NETBOX_URL=http://127.0.0.1:8123 invoke artifact-test` passed with status 0: the built `forward_netbox-2.7.4-py3-none-any.whl` installed into the exact runtime image on NetBox 4.6.6 with Branching 1.1.2 under Python 3.14, migrated through `forward_netbox.0050_contributor_baseline_cascade`, reported no unmade migrations, served all 7 installed menu routes with status 200, and the recorded runtime SBOM carries 156 components reporting forward_netbox 2.7.4, netbox 4.6.6, branching 1.1.2.
