# Release 2.6.0 - Enterprise Convergence And Ownership

## Goal

Ship 2.6.0 as a production handoff release on NetBox 4.6.5 with
`netbox-branching` 1.1.1. The release must resolve the reported scope, SNMP
endpoint, virtual-parent, module-bay, software-version, CVE/DLM, partial-merge,
and false-drift failure modes with persisted evidence. No known 2.6 correctness
work is assigned to a later release.

## Release Contract

- Exactly one native NetBox Branching branch is created per sync.
- Validation completes before branch provisioning.
- A partial merge records its failed rows, leaves the branch retryable, and
  cannot mark the ingestion merged, baseline-ready, or ownership-complete.
- Missing module bays are created in the active branch before their modules and
  merge natively on the exact runtime matrix.
- Scope, status-tag, and virtual-parent ownership is durable and per sync.
- Ownership claims use the ingestion primary key as generation plus the
  resolved snapshot ID.
- Managed assignments materialize from the union of current claims. The last
  claim controls removal; one sync cannot remove another sync's assignment.
- Parent conflicts remain durable evidence, preserve the current relationship,
  and block ownership finalization.
- Stale overlay jobs mutate nothing and request reconciliation for the latest
  completed generation.
- Sync/source deletion releases claims and rematerializes surviving ownership.
- Existing operator-created virtual contexts are never adopted as plugin-owned.
- Automatic orphan device deletion is unavailable; reviewed manual cleanup is
  the only deletion path.
- Drift, health, audit, UI, and support exports cannot report convergence while
  merge or required ownership reconciliation is incomplete.
- Every queued Forward job enforces a two-hour timeout minimum while preserving
  a larger operator-configured `RQ_DEFAULT_TIMEOUT`.
- The package contains no selectable direct-write backend and creates no
  execution-ledger rows.

## Constraints

- Require NetBox 4.6.5 and exactly `netboxlabs-netbox-branching` 1.1.1 in
  package resolution, CI, development, runtime diagnostics, and release proof.
- Preserve unrelated operator and feature tags.
- Do not infer historical per-sync claims during migration.
- Keep ownership tables in the main schema; branch schemas fake the ownership
  migrations.
- Serialize ownership mutation with the PostgreSQL advisory lock and row locks.
- Keep normalization in NQE and NetBox mutation in tested adapters.
- Do not commit credentials, customer identifiers, tag values, device names,
  network IDs, snapshot IDs, support rows, private communications, or images.
- Do not publish, tag, or release until the human operator separately
  authorizes it after acceptance evidence is complete.

## Touched Surfaces

- Forward source, sync, ingestion, ownership, identity, and preserved-assignment
  models plus their migrations.
- Single-branch planning, execution, merge finalization, recovery, post-sync,
  scope reconciliation, device identity, virtual-parent, module, DLM, and
  optional-plugin adapters.
- Sync, ingestion, drift, health, ownership, and support-export UI and commands.
- NQE query contracts, dependency ordering, plugin configuration, packaging,
  exact-runtime development images, CI, release tasks, and operator docs.
- Unit, migration, scenario, exact-stack, packaging, sensitive-content, UI,
  scale, failure-injection, and customer-equivalent acceptance tests.

## Approach

1. Pin and validate the exact NetBox and Branching runtime.
2. Add main-schema ownership models for managed tags, per-sync tag claims,
   plugin-owned virtual contexts, per-sync parent claims, and reconciliation
   state.
3. Add generation-stamped post-merge overlays with stale-job rejection,
   catch-up, pending/completed/failed state, and global serialization.
4. Materialize tags and parent relationships from current claim unions,
   including last-claim deletion and conflict failure semantics.
5. Release ownership transactionally on model and queryset sync/source delete.
6. Make migration initialize no claims and adopt no operator virtual contexts.
7. Make partial merge strict and retryable against the complete logical branch
   returned by Branching 1.1.1, rejecting any retry whose logical total changes.
8. Create missing module bays in the branch-native device/module path and prove
   device, bay, and module merge on the exact matrix.
9. Surface incomplete ownership and partial merge in drift, health, audit,
   ingestion issues, templates, and sanitized support output.
10. Add a strict ownership audit that can require zero nonterminal branches.
11. Restore the documented watch and warning-audit commands as read-only
    single-branch tools rather than ledger adapters.
12. Remove contradictory current-path documentation for branch shards,
    direct-write bootstrap, execution-ledger state, and deferred ownership.
13. Delete the no-op execution-ledger module, run/step choices, caller branches,
    empty health/support payloads, and tests for models removed by migration
    `0028`.
14. Remove retired backend constants, aliases, fallback imports, cache paths,
    column-filter fetch contracts, and persisted progress state. Retain only the
    current single-branch contract and its durable ingestion, core Job, branch,
    validation, issue, and ownership facts; migrations may only convert or
    delete retired state once.
15. Promote the module, routing, peering, DLM, and ACI integration paths that
    pass the exact all-plugin matrix to supported status, and state unsupported
    protocol/data boundaries as deliberate product scope rather than beta work.
16. Make a zero-debt architecture audit part of release acceptance: no active
    implementation plan other than this release plan, no no-op compatibility
    module, no current beta/deferred/future-work marker, and no known
    correctness or maintainability item.
17. Fail closed at every operational boundary: encrypt form credentials before
    strict client validation, preserve future scheduled producers, recover dead
    standing schedules from durable intent, enforce exact optional-plugin
    availability during execution, and bind release evidence to its real gate.
18. Make repository publication fail closed: scan current text plus every
    post-baseline changed blob, commit message, and annotated tag message;
    reject unreviewed binary content by exact path and SHA-256; require a
    nonempty private-pattern feed for push and release workflows; and bind the
    tracked history baseline to an external repository trust anchor.
19. Protect `main` and version tags with non-bypassable repository rules and
    require exact-commit NetBox CI plus Python and JavaScript CodeQL checks
    before the release controller can promote or tag a candidate.
20. Remove tracked development credentials for NetBox, PostgreSQL, and Redis.
    Generate unique per-clone Docker secret files with create-only semantics,
    preserve the active acceptance stack locally without restarting it, and
    reject tracked credential env files or assignments in the harness.
21. Authenticate private candidate fetches in the trusted PR scanner with its
    read-only workflow token, retain exact-head binding, and load the local
    development-secret helper relative to `tasks.py` in every import context.
22. Enforce a 7,200-second RQ worker timeout in the exact development and
    acceptance stack so interrupted large-branch retries cannot silently fall
    back to NetBox's five-minute default after container recreation.
23. Enforce the same 7,200-second minimum on each queued Forward job so
    production correctness does not depend on a customer changing NetBox's
    generic queue default; report both configured and effective values in
    health and support evidence.
24. Default the exact development and acceptance workers plus file handlers to
    `INFO`, preventing per-object Branching debug output from leaking inventory
    names or dominating large-baseline runtime; enforce it in the harness.
25. Make merged-branch cleanup transactional and set-based. Lock the ingestion
    and branch, fail closed unless the persisted branch is merged, delete the
    branch-owned AppliedChange and ChangeDiff indexes without ORM Collector
    hydration, retain NetBox ObjectChange audits, and roll back the complete
    cleanup if Branching row or schema teardown fails.
26. Reconcile scope-tag identity against both exact NetBox names and normalized
    slugs, preserve legacy operator slugs and assignments, reject split identity,
    and restrict positive claims to present NetBox devices while reporting
    absent Forward backfilled targets explicitly.
27. Match Forward's NQE contracts exactly: use asynchronous query execution,
    remove runtime parameters from the diff API, skip unsupported diff attempts
    for parameterized maps, and make strict-diff mode fail before an API call.
28. Report concurrent workload jobs in completion order with per-map elapsed
    time so a slow query is visible while it is running instead of being hidden
    behind input-ordered result iteration.
29. Validate all published bundled maps through Forward Query Debug. Treat
    `parallel_foreach` as optimizer evidence, retain declarative query source,
    and record semantic nonparallel causes rather than changing row contracts.
30. Normalize dependency workloads before branch planning: require both cable
    endpoints in authoritative full device/interface workloads, preserve exact
    existing cables, choose remaining candidates deterministically, and exclude
    OSPF rows whose local interface is absent even after supported aliasing.
31. Remove the duplicate query-preflight execution phase from ingestion,
    validation, smoke, and audit paths. Normal ingestion executes each enabled
    map once, keeps optional diagnostic NQEs in explicit validation/preview
    paths, removes retired preflight settings through migration, and reports
    hashed unique/repeated execution-signature counts in API usage evidence.
    Scheduled, webhook, and catch-up runs also no-op on an unchanged eligible
    baseline without dispatching query-backed overlays; only manual UI/API
    actions carry the explicit force-resync flag, and they fail explicitly
    while another run is active. Reject duplicate logical map executions before
    scheduling and persist usage evidence when dependency previews fail.
32. Normalize the full DLM CVE catalog workload to IDs referenced by the exact
    full in-scope Vulnerability workload before branch planning. Preserve the
    two concurrent queries so CVSS/vendor fields execute once per CVE rather
    than once per device-CVE relationship, leave partial/diff evidence
    conservative, and prove a full sync removes legacy unlinked catalog rows.
33. Add a durable per-sync and per-map workload baseline for parameterized full
    NQE execution. Canonicalize deterministic row identities, retain only the
    last successfully merged generation, derive changed upserts and explicit
    deletes locally when Forward's diff endpoint cannot accept the active query
    parameters, invalidate comparison when the parameter or identity contract
    changes, and prove failed or merely staged branches cannot advance state.
    Keep native Forward diffs for truly parameterless maps and preserve the
    zero-NQE same-snapshot producer no-op.
34. Treat console-endpoint and device serial behavior as release acceptance,
    not query-source assumptions. Prove scoped Avocent/Cyclades/AlterPath rows
    survive the endpoint probe, device workload, branch merge, ownership/tag
    overlays, and repeat no-op without an out-of-scope claim. Populate
    `Device.serial` from the already-fetched Inventory Item workload only when
    it contains one distinct Forward CHASSIS serial, retain
    multi-chassis serials on Inventory Items, and preserve existing endpoint
    serials when Forward has no authoritative endpoint serial value.
35. Make query execution mode authoritative per model. Resolve final context
    and scope parameters before scheduling, reject effective duplicate maps,
    run every sibling map as full when any map is parameterized, and fail the
    entire model before API execution when strict diff mode cannot satisfy that
    contract. Consolidate all full siblings into one durable state generation.
36. Gate dynamic-snapshot catch-up on completed ownership. Claim one durable
    catch-up check under the ownership lock, perform the Forward lookup outside
    the transaction, queue only after every required overlay completes, and
    make stuck recovery terminalize dead producers before retrying without
    passing the dead merge job as an active-job exemption.

## Validation

Development checks use the smallest exact-runtime test set after each change.
Release authorization requires all of the following against the final tree:

1. Focused merge, ownership, scope, virtual-parent, module, job, drift, health,
   export, migration, and operator-command tests.
2. `makemigrations --check --dry-run forward_netbox` and fresh forward/backward
   migration proof where the migration contract permits reversal.
3. Exact-matrix full Django suite and scenario suite.
4. `invoke harness-check` and `invoke harness-test`.
5. `invoke lint`, `invoke check`, `invoke scenario-test`, and `invoke test`.
6. `invoke docs` and link/build validation.
7. `invoke ci` from the same final tree.
8. Wheel and source distribution build, `twine check`, wheel installation into
   the exact runtime, and `manage.py check`.
9. Scale and injected-failure tests, including strict partial merge retry and
   cross-sync ownership concurrency.
10. Desktop and mobile UI checks for drift, ingestion, health, ownership, and
    merge controls with no overlap or horizontal overflow.
11. Strict `forward_ownership_audit --fail-on-inconsistent
    --require-no-open-branches` on the release testbed.
12. Sensitive-content scan and repository status review.
13. Validation-organization query audit when credentials and the approved
    environment are available.
14. Customer-equivalent acceptance: preview, sync, overlay completion, repeat
    same-snapshot sync, zero-change convergence, and sanitized support export.
15. Zero-debt architecture scan and independent diff review showing no current
    no-op shim, beta path, deferred work, known gap, or open maintenance item.
16. Runtime-retirement harness check proving removed aliases, cache keys,
    progress state, fetch modes, and import fallbacks cannot re-enter production
    plugin, query, package, development-configuration, or workflow files.
17. Recovery tests proving future scheduled producers remain queued and stale
    validation/preview occurrences are repaired without changing sync state.
18. Release-authorization negative tests proving unrelated successful commands
    cannot satisfy any evidence class.
19. Sensitive-content adversarial tests covering add-then-delete history,
    merge graphs, annotated tags, shallow clones, baseline advancement, missing
    secret configuration, output redaction, and binary allowlisting.
20. GitHub CodeQL checks for Python and JavaScript/TypeScript with zero open
    code-scanning alerts on the final candidate.
21. Customer-equivalent Avocent and serial proof: all scoped Avocent endpoints
    persist as Avocent Console Servers with their include tag and no
    out-of-scope tag; a serial-bearing scoped modeled device passes Forward
    Query Debug and persists its projected serial after merge.
21. Release provenance adversarial tests covering unreviewed ancestors,
    cross-workflow status forgery, wrong-PR run reuse, and review revocation
    after the first 100 review records.
22. Release supply-chain tests proving every Python controller dependency is
    transitively version/hash locked and two isolated wheel/sdist builds are
    byte-identical before artifact validation or publication.
23. Development-secret tests proving fresh generation, create-only reuse,
    restrictive permissions, symlink rejection, Compose file-secret wiring,
    and CI generation before the first Compose command.
24. Queue-dispatch tests proving a short NetBox default cannot shorten a
    Forward job and a larger operator timeout remains authoritative.
25. NQE execution tests proving asynchronous query use, completion-order
    workload telemetry, one logical execution per enabled map, zero
    parameterized diff requests, strict-diff pre-branch failure, parameterless
    diff success, and repeated-signature usage warnings.
26. Workload-normalization tests proving cable scope completeness, parent
    coverage, existing-cable preservation, deterministic conflict selection,
    OSPF alias coverage, and partial/diff evidence conservatism.
27. Durable workload-state tests proving deterministic identities, compressed
    round trips, parameter/contract invalidation, changed-row and delete
    derivation, successful-merge promotion, failed/staged non-promotion,
    same-snapshot local no-change behavior, and migration safety on Branching
    schemas.
28. Model-wide query-planning tests proving mixed full/diff execution is
    impossible, strict diff fails before any API call, and duplicate detection
    uses effective runtime parameters after context injection.
29. Ownership/catch-up tests proving an advanced snapshot cannot queue while
    any required domain is pending, only the final overlay claims the check,
    concurrent finishers cannot claim it twice, and recovery removes dead
    producer jobs before queuing replacement work.

## Evidence

Evidence captured on 2026-07-18 before the final full-gate rerun:

- Runtime reports NetBox 4.6.5 and `netbox-branching` 1.1.1.
- Live Query Debug accepted every published bundled query. Of 48 bundled query
  paths, 28 compiled with device-parallel `parallel_foreach`, one had no device
  scan, and 19 retained nonparallel plans because their exact semantics require
  multiple device scans, global grouping, or non-device-first sources. The 32
  enabled acceptance maps had zero explain failures; 15 were device-parallel,
  one had no device scan, and 16 had recorded semantic blockers.
- The customer-equivalent fetch proved bounded concurrency and asynchronous NQE
  execution: 32 workload jobs completed out of order with a pool of 10, and the
  run recorded 68 asynchronous query calls. It also exposed 32 rejected
  parameterized diff requests from the old client contract; the corrected
  client never emits that unsupported payload and focused regressions cover
  both fallback modes.
- Final customer-equivalent execution evidence on 2026-07-21 proves the
  consolidated contract. Explicit validation executed each of 32 enabled maps
  once with no preflight pass. The forced normal sync recorded 34 unique
  asynchronous NQE executions (32 maps plus the two required scope queries),
  zero diff calls, zero repeated signatures, zero HTTP failures, zero retries,
  and zero 429 responses. Its 152 applied changes merged and finalized with
  zero failed changes and zero ingestion issues.
- Dependency normalization reduced 24,791 cable rows to 17,283 authoritative
  candidates by removing 7,508 existing-endpoint conflicts, and reduced 3,190
  OSPF interface rows to 2,924 by removing 266 interfaces absent from the
  authoritative workload. Independent review found that the first unchanged
  producer still dispatched query-backed ownership overlays. The corrected
  boundary now terminates the complete job graph: the fresh live repeat created
  exactly one completed producer, zero child jobs, zero API usage evidence, and
  zero new ingestions.
- The synthetic Playwright harness can no longer target a shared runtime.
  `invoke playwright-test` always created and destroyed its isolated Compose
  project and Postgres volume, passed all 14 desktop/mobile checks, and left the
  acceptance database at one real source, one real sync, zero synthetic rows,
  zero active plugin jobs, zero ingestion issues, and zero open branches. The
  seed command also fails closed without the isolated-runtime marker.
- The post-cleanup strict ownership audit reported `consistent=true` and
  `release_ready=true`: all three required reconciliations completed, 3,820
  device-tag claims and 332 virtual-parent claims matched their assignments,
  and every missing, pending, conflict, mismatch, orphan, provenance, and open
  branch count was zero.
- Strict merge, ownership, jobs, scope, virtual-parent, and module integration
  focused suite: 42 passed.
- Exact branch-native module-bay integration proved two changes applied with no
  failed merge rows.
- Ownership lifecycle tests, including model/queryset delete release: 5 passed.
- Strict ownership command tests: 3 passed.
- Explicit bulk ORM disable regression: passed.
- Read-only single-branch operator command tests: 3 passed.
- Runtime code contains no retired progress, singular scope-tag, column-filter,
  diagnostic-cache, import-fallback, or Django-cache job-result path; a harness
  invariant now enforces that boundary while excluding one-time migrations.
- Sync logs and statistics persist only to NetBox core Job rows; no secondary
  cache or sync-primary-key compatibility path remains.
- Pre-commit formatting and lint hooks passed before the operator-command and
  final documentation changes; they must be rerun on the final tree.
- Independent review found six final-tree blockers in authentication validation,
  scheduled/standing recovery, optional-plugin enforcement, evidence binding,
  and retirement-scan coverage. All six received production fixes and explicit
  regressions; the exact NetBox 4.6.5 focused run passed 45 tests, harness tests
  passed 89 tests, and the expanded harness/lint/diff checks passed. Independent
  corrective-diff review and every full release gate remain intentionally
  unchecked until they run on the final tree.

Final-tree evidence captured on 2026-07-19 after the crash-resume and health
probe fixes:

- Exact runtime system check passed on Python 3.14.4, NetBox 4.6.5, and
  `netbox-branching` 1.1.1.
- The complete CI mirror passed from the pre-review tree: 103 harness
  tests, 34 isolated scenario tests, 1,345 isolated Django tests with three
  intentional skips, strict sync release-health checks, lint, documentation,
  14 desktop/mobile UI checks, and package build. The final review corrections
  require one more complete CI mirror before release authorization.
- A separate exact-runtime full suite also passed 1,344 tests before the final
  lock-contention regression was added; the final CI run supersedes it.
- The explicit scale suite passed at its unreduced defaults: 20,000-row bulk
  staging and merge, 20,000-prefix create/update/delete, and a 20,005-change
  create plus complete crash-resume replay. After independent review identified
  a retry-timeout risk, provenance was batch-loaded under ordered destination
  row locks and the no-op path stopped deserializing already-attested rows. The
  corrected concurrent run merged in 102.53 seconds and replayed in 56.88
  seconds, projecting a one-million-row retry at 2,843.44 seconds. The test now
  rejects projections at or above 5,400 seconds, preserving 25 percent headroom
  under the documented 7,200-second worker timeout. The canonical isolated
  release task independently passed with a 57.10-second replay and 2,854.23-
  second projection; the task is now required by both `invoke ci` and formal
  `scale-and-failure` release authorization.
- Crash-resume integration now proves Region, DeviceRole, Platform, and Site
  replay, rejects an unrelated same-primary-key object, rejects unaudited state
  divergence, and holds the destination row lock across provenance validation
  and convergence against a competing database connection. The concurrency
  test now requires PostgreSQL `pg_blocking_pids()` evidence instead of relying
  on thread timing.
- The built 2.6.0 wheel and source distribution passed `twine check`. A clean
  wheel-only install applied every migration, passed system and migration-drift
  checks, and produced a validated 157-component CycloneDX runtime SBOM.
- The architecture-completion gate passed all five model/fetch/query contract
  tests. The strict release-testbed ownership audit reported consistent state,
  zero open branches, zero pending or failed reconciliation, and
  `release_ready=true`.
- Independent final-tree review found three P1/P2 gaps: retry timeout headroom,
  a scheduler-sensitive contention assertion, and a text-only health-probe
  harness check. Re-review then found deleted-row resurrection, incomplete
  formal scale authorization, batched-lock coverage, and scheduled PyYAML
  installation gaps. All findings have production fixes and regressions: prior
  branch-created rows fail closed when deleted, both Site and Region paths have
  database-confirmed lock tests, release authorization requires the canonical
  retry benchmark, and the gardening workflow installs pinned PyYAML before its
  parsed harness check. A final independent review reports no P0-P2 blockers;
  110 harness
  tests, all 52 focused bulk-merge tests, and the corrected unreduced scale test
  pass. The three new deletion/lock regressions also pass on the exact runtime;
  the final complete CI mirror remains required after these corrections.
- The long-lived customer-equivalent ingestion contains 750,069 logical
  objects. Its recovered old-code merge remains active; the latest bounded
  sample showed 492,607 branch-applied audit records and ongoing inventory-item
  application. It must finish its
  partial attempt, restart idle workers on the final code, retry the same
  branch, complete overlays and issue audits, and pass same-snapshot convergence
  before customer acceptance or release authorization can be recorded.
- The 750,069-object recovery then exposed 294 new resume conflicts, all on
  Device `module_bay_count`. NetBox 4.6.5 identifies it as a non-editable
  `CounterCacheField`, and later branch-owned ModuleBay creation legitimately
  changes it after the Device create audit. Resume provenance now treats that
  exact field like the already-covered `inventory_item_count`; a full
  Device-plus-ModuleBay interrupted-create regression proves retry acceptance
  while writable Device drift remains fail-closed.
- A host reboot recreated the exact acceptance workers with the shipped
  `RQ_DEFAULT_TIMEOUT=300`, and the next supported branch retry failed closed at
  exactly five minutes. The testbed environment and configuration fallback now
  require 7,200 seconds, and the harness rejects a missing, short, or duplicate
  timeout assignment before CI or release publication. That timeout also proved
  the handler was consulting a stale in-memory Branch status after the merge had
  moved the database row to `merging`; retry and terminal-failure transitions now
  update the authoritative row conditionally and have stale-cache regressions.
- Queue-boundary review then proved that the development default alone did not
  protect production deployments. Forward jobs now carry an explicit 7,200-
  second minimum timeout, larger operator values are preserved, and health plus
  support evidence distinguish the configured NetBox default from the effective
  Forward timeout.
- The same live retry showed the worker override forcing `DEBUG` and both file
  handlers bypassing the configured level, emitting per-object merge details.
  The exact testbed now uses `INFO` consistently, and harness tests reject a
  debug worker, debug handler, or debug default.
- Final recovery review found that durable merge finalization was evaluated
  after stale branch-state repair. Recovery now checks `merge_finalized_at`
  first under the ingestion/sync/branch lock order, preserves the stale branch
  row without mutation, and restores the authoritative sync state to
  `completed`. Exact regressions cover finalized ingestions with both `merging`
  and `ready` branch rows.
- The recovered customer-scale merge then reached its exact 7,200-second
  boundary inside resumed relationship validation. Broad row-isolation handlers
  swallowed `JobTimeoutException`, leaving PostgreSQL's transaction broken; a
  later query surfaced `TransactionManagementError` and incorrectly marked the
  branch failed. All bulk staging, bulk merge, and framework fallback isolation
  boundaries now re-raise RQ timeout control flow immediately, while sync
  orchestration persists the timeout state and lets the job layer record the
  timeout issue. Exact regressions prove the timeout identity survives resumed
  relationship validation, the database connection remains usable, the branch
  returns to retryable state, finalized state is preserved, and sync staging
  records `timeout` rather than a generic failure.
- The corrected exact NetBox 4.6.5, Branching 1.1.1, and Python 3.14 focused
  suite passed all 63 merge-recovery and Forward job-queue tests, followed by
  all seven targeted timeout-propagation tests and the complete 121-test
  bulk-apply, bulk-merge, job-entrypoint, and orchestration matrix. The harness
  passed all 201 tests, lint and formatting passed, the architecture harness
  passed, documentation built successfully, and final corrective-diff review
  reported no P0-P2 findings. The final full gate remains required.
- A repository-wide follow-up audit then found additional worker-reachable
  best-effort exception handlers outside merge/apply that could consume the
  asynchronous RQ deadline. Job lifecycle, source credential, API cache,
  query-resolution, diagnostics, logging, liveness, model, reporting, and
  integration-adapter boundaries now preserve `JobTimeoutException` identity.
  `scripts/tests/test_job_timeout_boundaries.py` statically enforces that
  invariant for all job, model, and worker utility modules; request-only health
  rendering is intentionally outside the RQ worker surface. The harness now
  passes all 202 tests, the custom runner lifecycle regression passes on exact
  NetBox 4.6.5 and Branching 1.1.1, and the complete 375-test affected matrix
  covering jobs, models, logging, Forward API, credentials, query binding and
  registry, liveness, primary-IP handling, runner contracts, and stability
  hardening also passes. Independent corrective-diff review again reported no
  P0-P2 findings.
- Live log review exposed two testbed containment defects before release. The
  Compose worker command expanded `FORWARD_NETBOX_WORKER_AUTORELOAD` on the host,
  so a service-level `0` override still launched `watchmedo`; source changes
  restarted the parent worker and left the active RQ work horse detached. An
  isolated Django test also placed a synthetic RQ job where a shared worker
  could consume it. Compose now passes the setting into the container and
  defers shell expansion until runtime. Isolated Django tests reserve Redis
  databases 14 and 15 for tasks and cache as a second containment boundary.
  Harness tests reject either regression, and functional Compose process
  inspection proves the disabled worker launches `rqworker` directly without
  `watchmedo`. The exact lifecycle test then passed with task/cache Redis
  databases 14/15 in its real process environment; shared databases 0 and 14
  received no new test job, and the disposable stack was removed. Independent
  review of the Compose interpolation and Redis containment patch reported no
  P0-P2 findings.
- Throughput evidence from the 750,069-change retry showed that a fixed
  7,200-second minimum still made timeout/retry an expected path for a large
  baseline. Merge dispatch now counts the persisted unmerged branch changes and
  derives a conservative RQ deadline from the same per-change estimate and 80%
  safety ratio used by capacity guidance. The configured timeout remains the
  floor, so a larger operator value is never reduced. Tests cover the 7,200-
  second floor, a 750,000-change 75,000-second deadline, a larger configured
  timeout, queue dispatch, and merge enqueue wiring.
  The exact NetBox 4.6.5 and Branching 1.1.1 queue/merge matrix passed all 32
  tests, and independent review reported no P0-P2 findings. A real-Redis
  regression also verifies the persisted RQ job carries the larger workload
  deadline.
- The 750,069-change merge completed with zero failed changes, but supported
  post-merge recovery exposed a cleanup-scale defect before release:
  `Branch.delete()` attempted to hydrate 1,384,780 AppliedChange rows and
  750,069 ChangeDiff rows through Django's cascade Collector. The interrupted
  cleanup left the merged branch and all evidence intact. Cleanup now uses two
  transactional set deletes before normal Branching teardown, preserves core
  ObjectChange audits, and has rollback, ordering, stale-state, and terminal-
  state regressions. Live repair and full final-tree gates remain required.
- The first ownership retry then exposed an existing include tag whose exact
  name used a noncanonical NetBox slug. Exact-name adoption now preserves that
  tag and its operator assignments; a name/slug split fails closed. The next
  retry proved that all 3,415 completed devices and 307 imported endpoints were
  present, while 37 absent targets were Forward backfilled records with no
  NetBox row. Reconciliation now reports and excludes only those nonexistent
  targets from positive claims while retaining strict identity checks for all
  3,722 present targets. Exact-runtime regressions pass; live ownership retry
  and full final-tree gates remain required.
- The corrected live ownership retry completed all three domains with 3,722
  durable device identities, 3,723 scope claims across two managed tags, zero
  missing assignments, zero parent conflicts, and zero open branches. The
  strict ownership audit reported `consistent=true` and `release_ready=true`.
  Tag-job result evidence now derives `ownership_current` from the finalized
  scope and status domains, so an unused backfilled/out-of-scope tag type cannot
  leave a successful job carrying a false incomplete flag.

The earlier 2.6 candidate's results remain historical only. The evidence above
belongs to the current working tree but does not authorize a release: customer
acceptance and the final complete CI mirror are incomplete, and no evidence-base
commit has been selected.

Release-controller corrective evidence captured on 2026-07-20:

- Independent rereview found four release-path blockers: the verifier did not
  cover ancestors before the production PR, a different Actions workflow could
  forge the shared bot/context status, only the first 100 reviews were read,
  and release tooling was installed from mutable latest packages.
- The corrected verifier proves the only prior post-release commit is a
  documentation-only bridge and validates the reviewed security bootstrap plus
  every first-parent main commit through the release evidence commit.
- Trusted scanner statuses now carry the exact Actions run URL. Provenance
  validation binds that URL to the active trusted workflow ID, successful
  `pull_request_target` run, main-targeting PR number, and exact candidate SHA.
  All review, PR-association, and status lists are fully paginated.
- Nine provenance adversarial tests, two reproducible-build tests, and 34
  harness tests pass. A fresh Python 3.14 environment installed the complete
  transitive release toolchain from `requirements-release.txt` with
  `--require-hashes` and no exception. Two independent builds produced
  byte-identical wheel and sdist SHA-256 digests. Bootstrap PR review, final
  independent rereview, and all final release gates remain required.
- The final contract audit found one stale validation-matrix sentence that
  still advertised the four retired no-op ACI model families. The matrix now
  matches the executable integration registry, and the harness requires the
  explicit 2.6 exclusion for application profiles, EPGs, contracts, and static
  port bindings.
- The same audit compared bundled NQE to `v2.5.11` and found that the release
  notes incorrectly exempted 2.5.11 upgrades from query publication even
  though two 2.6 device-model queries contain behavior changes. All current
  upgrade, troubleshooting, compatibility-table, and changelog guidance now
  requires one overwrite publication for every pre-2.6 source, and the harness
  requires that upgrade instruction.
- The final publication design uses the repository's normal GitHub/PyPI path:
  a maintainer runs the live-control preflight from reviewed `main`, creates an
  annotated `v*` tag at the exact reviewed evidence commit, pushes it through
  ordinary Git, and verifies the remote peeled target. Version tags reject
  deletion and movement. The tag-triggered workflow re-proves reviewed lineage,
  runs the exact release gate, publishes through PyPI OIDC, and creates the
  GitHub Release from the same tested artifacts.
- CodeQL identified two high-severity clear-text logging flows in privileged
  release code. The authorizer now emits only a fixed success message and the
  release command runner never logs command arguments or includes them in
  errors; regression tests inject secret-shaped values and prove they remain
  absent.
- Final independent rereview found that the trusted private-pattern status was
  not yet required by `main`. The bootstrap permits its own four public
  statuses; version authorization requires the authenticated private-pattern
  status as a fifth main rule. The local preflight fail-closes on live
  repository, ruleset, PyPI environment, deployment-policy, and Actions
  SHA-pinning drift. The PyPI environment disables administrator bypass.
- The first metadata promotion exposed contradictory compatibility prose: all
  three tables named 2.6.0 current while their introductions still called it a
  candidate. Release preparation and promotion now update the canonical
  introduction and table together, fail closed if that introduction is absent
  or duplicated, and have regressions for candidate, published, idempotent, and
  malformed input.
- Live probing also found that the workflow waiter placed full repository paths
  in GitHub's workflow-ID URL, producing 404 responses that appeared as missing
  runs until timeout. It now queries by workflow filename, still validates the
  returned full workflow identity, and fails immediately on a redacted API
  error; empty, malformed, and wrong-schema JSON also fail without echoing the
  response, and the release-workflow waiter uses the same checked paths. Poll
  bounds now allow 80 minutes for exact branch checks and 120 minutes for the
  full tag-triggered CI, artifact, PyPI, and GitHub publication sequence.

Final correctness review evidence captured on 2026-07-21:

- Independent review found six P1/P2 issues: disabled peer syncs could suppress
  local deletes; mixed parameterized full and native-diff maps could bypass
  durable state; duplicate detection used stored rather than effective
  parameters; durable full ACI state could derive deletes despite the ACI
  safety policy; catch-up could queue before ownership overlays; and recovery
  could leave a dead producer active while requesting catch-up.
- The corrected planner makes execution mode and duplicate identity
  model-wide. The durable reducer includes parameterless full siblings, filters
  peer protection by enabled model, and preserves ACI state until the operator
  enables ACI deletion. The post-merge state machine claims catch-up only after
  ownership convergence and recovery completes dead producers first.
- Exact-runtime focused validation passed 90 workload-state, query-registry,
  and ACI tests; three mixed-planner tests; and five ownership/catch-up/recovery
  regressions. The advanced-snapshot test proves zero catch-up decisions while
  either ownership domain is pending and exactly one queued decision after the
  last domain completes. The final broad gates remain required.
- Customer-equivalent ingestion 20 staged and merged eight stale, exclusively
  plugin-owned device deletes and 39 orphan CVE deletes with zero failed changes
  and zero ingestion issues. The guarded device path retained every identity
  protected by a current scope claim, preserved assignment, peer identity, or
  virtual-parent relationship. The testbed then reported zero stale exact
  device identities, 306 Avocent console servers, and 306 matching scope claims.
- Customer-equivalent ingestion 21 converged the DLM catalog by deleting three
  unreferenced legacy SoftwareVersions while retaining all 45 versions referenced
  by DeviceSoftware or Vulnerability workloads. The final DLM state contained
  313 CVEs, zero CVEs without a Vulnerability, zero CVEs without CVSS evidence,
  and 36,595 Vulnerability rows.
- Forced customer-equivalent ingestion 22 was a true no-op: zero applied,
  created, updated, deleted, failed, and issue counts. It issued 34 unique NQE
  signatures exactly once each, with zero repeated signatures, and passed the
  API-usage budget. Of 3,305 DeviceSoftware-associated devices, 3,299 received
  the single authoritative chassis serial from source evidence; the six without
  serials had no distinct source chassis serial to project.
- The final live strict release gate reported zero blocking findings, warnings,
  suppressed findings, and errors. Ownership for generations 21 and 22 was
  complete with zero integrity issues and zero open branches. Focused final-tree
  validation passed 89 branch/merge/DLM/ownership/upgrade regressions plus 56
  workload-normalization, durable-state, and DLM regressions. The final complete
  CI, artifact, scale, UI, and independent-review gates remain required.
- A final DLM review found two one-snapshot correctness gaps: catalog protection
  was consulting pre-branch database associations, and deleting one
  Vulnerability did not remove its stale individual SoftwareVersion from the
  CVE's derived affected-software relation. Catalog protection now uses the
  authoritative current-plus-peer DeviceSoftware and Vulnerability targets,
  and Vulnerability deletion removes exactly the version no longer represented
  for that CVE. A real branch deleted Vulnerability, DeviceSoftware, CVE, and
  SoftwareVersion in one merge with four applied changes and zero failures.
- A concurrent-delete review then tested the interval after Django Collector
  discovery and exposed a late reverse-relation write that could escape the
  discovered on-delete policy. Authoritative Device deletion now acquires a
  deterministic reverse-table write barrier after the ownership advisory lock
  and before Collector discovery. PostgreSQL-confirmed concurrency tests prove
  that both a late ownership claim and a late Device relation block, then fail
  with integrity errors after deletion without leaving claims, identities, or
  related rows.
- Exact NetBox 4.6.5, Branching 1.1.1, and Python 3.14 validation passed all 127
  combined workload-state, DLM, and production merge tests after these fixes.
  Sensitive-content, formatting, lint, harness, NetBox system, and diff checks
  also pass. The complete final-tree CI, artifact, and independent-review gates
  remain intentionally required before authorization.
- Corrective rereview found that the first Device barrier omitted Django's
  hidden Collector relations and private GenericRelations, and that a blocking
  multi-table lock could deadlock with an ordinary reverse-order writer. Table
  discovery now mirrors `get_candidate_relations_to_delete()` plus Collector's
  private `bulk_related_objects` traversal. Both merge paths share one bounded
  `NOWAIT`/savepoint/retry primitive, and the existing reverse-order PostgreSQL
  concurrency test passes against that shared implementation.
- Because GenericForeignKey storage has no physical Device foreign key, table
  serialization alone could allow a blocked writer to resume after deletion.
  Migration 0042 installs an insert/update trigger on all eight current Device
  GenericRelation tables, with post-migrate installation for the optional
  routing table. The exact-runtime race test proves a direct TaggedItem writer
  blocks and is then rejected after Device deletion, while a separate migration
  test proves a generic relation cannot target a missing Device. All five
  ownership, generic-relation, and reverse-lock safety proofs pass.
- A second corrective review found that the explicit reviewed-orphan prune path
  still performed a direct Device delete without the new relation barrier. It
  now takes the same ownership lock and Collector-complete barrier before every
  Device delete. Its PostgreSQL concurrency test proves a late TaggedItem writer
  blocks, the prune completes, and the trigger rejects the writer without a
  dangling row. A source audit found no other production direct Device delete
  call site.
- Availability rereview then found that reviewed prune retained its table locks
  across the complete orphan list. Prune now opens one ownership/barrier/delete
  transaction per Device, using a private lock-held ownership primitive with an
  atomic-block assertion instead of nested lock wrappers. A two-Device
  PostgreSQL test pauses between transactions and proves a relationship writer
  completes before the second deletion begins, bounding write interruption to
  one Device while preserving atomic ownership rollback.
- The per-Device transaction change initially made a reviewed virtual
  parent/child set dependent on PK order. Prune now topologically orders current
  virtual-parent claims child-before-parent and fails closed on cycles. An exact
  runtime regression with the parent deliberately assigned the lower PK deletes
  child and parent in one run with zero ownership-blocked devices and no
  remaining identity or parent claim. Temporarily blocked identities are
  reordered after any pass that made progress, closing concurrent claim changes
  without retrying permanent external ownership indefinitely.
- Two independent final-tree rereviews report no P0, P1, or P2 release blockers.
  The second reviewer independently passed 12 focused PostgreSQL tests on
  NetBox 4.6.5 and Branching 1.1.1, including permanent cross-sync protection,
  all reviewed-prune regressions, merge races, reverse-order lock retry,
  migration guards, and DLM convergence; all eight GenericRelation triggers
  were installed and diff hygiene was clean.
- The final-tree canonical CI passed 1,481 Django tests with three expected
  skips, the strict sync release gate, 20,005-row merge scale validation, all
  14 desktop/mobile Playwright checks, documentation, and reproducible wheel
  and source builds. The one-million-row retry projection was 1,734.64 seconds
  against the 2,400-second limit. A separate wheel-only clean-install gate
  applied every migration through 0042 on NetBox 4.6.5 and Branching 1.1.1,
  passed system and migration-drift checks, and produced a 157-component SBOM.
- The first remote release SHA exposed a local/CI pre-commit parity gap only:
  the pinned import-order and Black hooks changed five workload-state and
  migration files without semantic changes. Running the exact hooks locally
  converged cleanly, and all 24 affected workload-state and migration tests
  passed on the exact PostgreSQL runtime before the formatting correction was
  amended with this plan evidence.
- The protected production PR then surfaced ten open GitHub Advanced Security
  CodeQL alerts that the ordinary workflow conclusion did not expose: raw
  caught-exception text reached API responses, Django messages, support-bundle
  health/query diagnostics, and one archive response, while harness tests wrote
  synthetic GitHub secret expressions to temporary workflow files. External
  responses now use fixed operator-safe messages and retain only stable status,
  remediation, and job identifiers; server logs retain the failed operation and
  exception class without the exception message or traceback payload.
  Harness workflow fixtures are parsed in memory and write only a non-sensitive
  tasks fixture. Security regressions inject sentinel exception content and
  prove it cannot cross the HTTP or support-evidence boundary.
- Independent security rereview found two additional P1 boundaries: merge and
  runner exceptions could persist raw text or tracebacks in NetBox Job data,
  and three custom ingestion HTMX routes required login but not the native
  object view permission. Persisted job failures now retain only fixed operation
  text and exception type; historical failure entries are sanitized before UI
  rendering or export; merge issues retain aggregate model/type evidence without
  traceback content; and logs, progress, and change-diff routes all enforce
  `forward_netbox.view_forwardingestion`. Sentinel and permission regressions
  cover both existing records and newly raised failures.
- The follow-up security rereview found four adjacent paths before commit:
  ordinary row failures retained exception text, traceback logs, and source row
  values; warning-level NQE failures bypassed historical presentation redaction;
  tag-scope preview rendered raw Forward errors; and archive passwords remained
  accepted in GET query strings. Unexpected row issues now retain only model,
  exception type, and field/type shape, while expected dependency-skip rollups
  retain their aggregate remediation evidence. Warning/error presentation,
  scope-preview errors, and bundle-password transport are covered by sentinel
  regressions, with encrypted archives accepting passwords only by POST.
- Final security hardening removed the remaining raw dependency-skip row
  messages, source-row context, primary-IP warning text, and filtered exception
  summaries. Persisted failure evidence now contains only stable exception
  classes plus mapping field-shape metadata; source discovery requires a saved
  source ID and never transports credentials through query strings.

## Release Authorization

The local finalizer and release workflow reject the release while any required item
is unchecked or lacks concrete evidence. These entries are updated only from the
same final tree that will be tagged.

- Evidence base commit: `49d224261bf5775edb769a58770ac10021ee6bbb`

- [x] `final-tree-full-gate` - `rtk env FORWARD_NETBOX_DOCKER_PROJECT=forward-netbox-release-gate FORWARD_NETBOX_POSTGRES_DATA_PATH=netbox-postgres-data FORWARD_NETBOX_WORKER_AUTORELOAD=0 NETBOX_VER=v4.6.5 FORWARD_NETBOX_HOST_PORT=8080 NETBOX_URL=http://127.0.0.1:8080 invoke ci` completed green: harness, lint, checks, scenario, Django, docs, sensitive-content, and CI-equivalent gates passed with 0 failures.
- [x] `exact-runtime-artifact` - `rtk env FORWARD_NETBOX_DOCKER_PROJECT=forward-netbox-release-gate FORWARD_NETBOX_POSTGRES_DATA_PATH=netbox-postgres-data FORWARD_NETBOX_WORKER_AUTORELOAD=0 NETBOX_VER=v4.6.5 invoke artifact-test` passed clean installation, migration, system checks, metadata, and installed-runtime SBOM validation on NetBox 4.6.5, Branching 1.1.1, and Python 3.14.
- [x] `scale-and-failure` - `rtk env FORWARD_NETBOX_DOCKER_PROJECT=forward-netbox-upgrade26 FORWARD_NETBOX_POSTGRES_DATA_PATH=netbox-postgres-data FORWARD_NETBOX_WORKER_AUTORELOAD=0 NETBOX_VER=v4.6.5 invoke scale-soak --runs 3 --max-changes-per-staging-item 10000 --pause-seconds 0`, `rtk env FORWARD_NETBOX_DOCKER_PROJECT=forward-netbox-release-gate FORWARD_NETBOX_POSTGRES_DATA_PATH=netbox-postgres-data FORWARD_NETBOX_WORKER_AUTORELOAD=0 NETBOX_VER=v4.6.5 invoke scenario-test`, and `rtk env FORWARD_NETBOX_DOCKER_PROJECT=forward-netbox-release-gate FORWARD_NETBOX_POSTGRES_DATA_PATH=netbox-postgres-data FORWARD_NETBOX_WORKER_AUTORELOAD=0 NETBOX_VER=v4.6.5 invoke bulk-merge-retry-scale-test` passed scale, collision, rollback, partial retry, stale-generation, concurrency, and recovery coverage with 0 errors.
- [x] `ui-validation` - `rtk env FORWARD_NETBOX_DOCKER_PROJECT=forward-netbox-release-gate FORWARD_NETBOX_POSTGRES_DATA_PATH=netbox-postgres-data FORWARD_NETBOX_WORKER_AUTORELOAD=0 NETBOX_VER=v4.6.5 FORWARD_NETBOX_HOST_PORT=8080 NETBOX_URL=http://127.0.0.1:8080 invoke playwright-test` passed desktop and mobile validation with 14 checks and 0 overlap, overflow, blank-state, or broken-action findings.
- [x] `ownership-audit` - `rtk docker compose --project-name forward-netbox-release-gate --project-directory development exec -T netbox python manage.py forward_ownership_audit --fail-on-inconsistent --require-no-open-branches` reported consistent=true, release_ready=true, 0 inconsistent records, and 0 open branches.
- [x] `customer-equivalent-acceptance` - `rtk env FORWARD_NETBOX_DOCKER_PROJECT=forward-netbox-upgrade26 FORWARD_NETBOX_POSTGRES_DATA_PATH=netbox-postgres-data FORWARD_NETBOX_WORKER_AUTORELOAD=0 NETBOX_VER=v4.6.5 invoke sync-release-gate --sync-ids 51 --max-polls 6 --interval-seconds 10` passed sync id 51 preview, sync, overlays, issue assertions, support evidence, and same-snapshot repeat convergence with 0 warnings and 0 blocking issues.
- [x] `independent-review` - Independent review passed with 0 blockers and 0 findings after `rtk git diff --check`, the security hardening, query publication repair, and dependency-summary corrections.

## Rollback

### Upgrade Procedure

1. Back up NetBox and quiesce Forward sync workers.
2. Resolve or explicitly preserve every nonterminal Forward branch before
   migration; release acceptance requires zero open branches.
3. Install NetBox 4.6.5, Branching 1.1.1, and the 2.6.0 candidate together.
4. Run migrations and restart NetBox services and workers.
5. Run `manage.py check` and the strict ownership audit.
6. Run every relevant sync to establish current generation claims; migration
   deliberately invents no historical claims.
7. Wait for all required ownership domains to complete before accepting drift
   convergence.

### Rollback Procedure

Rollback across the ownership schema boundary is backup restore, not a mixed
package downgrade. Restore NetBox, plugin, Branching, database, and worker state
as one tested unit. Do not run an older plugin against 2.6 ownership state.

## Decision Log

- Use exactly NetBox 4.6.5, Branching 1.1.1, and Python 3.14 so development,
  CI, packaging, and release evidence prove one runtime instead of a range.
- Use one native branch per sync; rejected direct-write and secondary execution
  ledgers because they create alternate state and merge semantics.
- Persist ownership and device identity in the main schema; rejected inferred
  historical claims because they could transfer control of operator data.
- Finalize merge baseline and ownership registration atomically; rejected
  best-effort post-merge state because it can report false convergence.
- A configured include tag is authoritative plugin-managed scope on migration,
  so migration invents neither claims nor preserved assignments for it. Later
  explicit adoption of a different pre-existing tag records its current
  assignments; explicit operator removal clears that preservation.
- Keep release evidence local and sanitized summaries tracked; rejected raw
  tenant artifacts because repository history is permanent.
- Treat stored schedule intent and canonical optional-plugin capability status
  as execution contracts. A missing worker occurrence is repaired without
  mutating sync state, while a missing or mismatched optional package is blocked
  before any query or adapter execution.
- Bind each release-authorization entry to its canonical command and expected
  evidence vocabulary; rejected generic command-success text because it cannot
  prove customer acceptance, ownership, UI, artifact, scale, or review gates.
- Keep the protected-history baseline externally approved and stable across
  ordinary releases; rejected a candidate-controlled trust anchor because it
  could exclude its own earlier commits from scanning.
- Permit binary documentation assets only by reviewed path and exact SHA-256;
  rejected extension-based or path-only exceptions because changed screenshots
  and non-UTF-8 artifacts are otherwise uninspectable by the text scanner.
- Require all repository-rule checks on the exact candidate SHA before main or
  tag promotion; rejected branch-level workflow success as insufficient proof
  when multiple workflows can run for the same branch.
- Require production and evidence-only PRs, bind the trusted base-branch
  scanner result to each final candidate SHA, and have the release workflow
  re-prove exact merged-PR association, commit verification, and CI/CodeQL
  workflow identities for both main commits.
- Use the standard annotated-tag release path from protected `main`, guarded by
  a local live-control preflight, immutable tag rules, exact remote-target
  verification, reviewed-lineage verification in Actions, and PyPI Trusted
  Publishing. The rejected App/deploy-key controller added operational secrets
  and a second publication path without improving the ordinary maintainer
  workflow requested for this repository.

## Release Acceptance

1. The exact dependency matrix is installed and reported at runtime.
2. All validation-ladder items pass on the final commit.
3. No migration drift, nonterminal branch, pending/failed required ownership
   reconciliation, merge failure, blocker issue, or sensitive-content finding
   remains.
4. A first baseline creates one branch, merges cleanly, and completes required
   ownership domains for its exact generation.
5. A second sync against the same resolved snapshot reports zero applied,
   created, updated, deleted, and failed changes.
6. Shared managed tags survive until the final claim is removed.
7. SNMP endpoints obey include/exclude scope, CIMC endpoints do not become
   standalone parentless devices, and virtual contexts preserve exact parent
   ownership.
8. Module inventory creates native bays without an operator pre-seed step.
9. CVE/DLM and software-version relationships appear only from the approved
   published query contracts and link to the intended device/software objects.
10. Support output contains actionable aggregate evidence without private
    inventory rows or credentials.
11. Current architecture, code, tests, and documentation contain no known
    technical debt, architecture gap, beta path, or deferred release work.
12. No tag or release is created until the operator explicitly authorizes it.

## Final Evidence Binding

The final release-evidence commit contains this plan only and binds the
validated protected-main tree to the `v2.6.0` publication gate.
