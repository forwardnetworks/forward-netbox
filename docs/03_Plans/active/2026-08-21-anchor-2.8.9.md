# Advance the release provenance anchor to 2.8.9

## Goal

Move the provenance anchor onto the shipped 2.8.9 release and promote it to
current in the compatibility tables.

## Why

The anchor names the previous release and the documentation-only bridge commit
that must directly follow its tag; until it advances, `check_harness.py` fails
by construction, which is the check working.

v2.8.9 published: the tag peels to `58242a030250c0cde10365f3bbb1b32cf9258cd1`,
the release workflow succeeded, and PyPI and GitHub carry byte-identical
artifacts (wheel `6464ae9e0fcf225f…`, sdist `0970f992f4e34ffe…`), verified by
downloading the GitHub assets and hashing them.

## Constraints

- The bridge must be the first commit after the tag on the first-parent lineage
  and documentation-only. `1f69b97` is one file, no code.
- The table promotion belongs here, not on the release branch.

## Touched Surfaces

- `scripts/verify_release_provenance.py`, `scripts/check_harness.py`,
  `scripts/tests/test_tasks.py`, `.github/workflows/release.yml`
- `README.md`, `docs/README.md`, `docs/01_User_Guide/README.md`, `CHANGELOG.md`

## Approach

Anchor to `v2.8.9` / bridge `1f69b97903e5423e0f70ebbbcdd11127f106ba86`; promote
2.8.9 to current, demote 2.8.8 to superseded.

## Validation

`check_harness.py` passes with the anchor advanced; `scripts/tests` passes;
`verify_release_provenance.py --controls-only` passes against live GitHub.

## Rollback

Revert; the harness fails again, which blocks the next release rather than
permitting an unverified one — the safe direction.

## Decision Log

- Promotion folded into the anchor commit, as for 2.8.6-2.8.8.

## Open

- The deployment that surfaced the delete-path defects should see its 16
  recurring skips fall to zero on 2.8.9: held rows are now reported as
  `ownership_quarantine_held_rows` rather than staged and refused.
