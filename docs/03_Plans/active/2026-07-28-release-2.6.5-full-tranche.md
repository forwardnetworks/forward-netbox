# 2.6.5 Full Tranche

## Goal

Clear the deferred backlog in one release: protected-delete ordering, diff
enablement for the models it blocks, the three proven performance engines, and
the remaining smaller items.

2.6.4 shipped the query-ID binding correction and is treated as burned; 2.6.5
is the substantive release.

## Sequencing

Dependency-first, because correctness work must land before the large
performance diffs destabilise it:

1. **Protected-delete convergence** (gating). Unblocks item 2.
2. **Delete-correctness comparator** from the diffproof tree. Needed to trust
   item 1 and to validate item 3.
3. **Diff enablement** for the models item 1 blocks.
4. **Performance engines** - set-based merge, then COPY/SQL staging. Largest
   and most conflict-prone; land last.
5. **Smaller items** and release.

## Scope and current state

### 1. Protected-delete convergence (gating)

Canonical delete intent is exact: 1,434 expected, 1,434 matched, 0 spurious, 0
missing. Physical convergence still fails: Device 6 to 5, Interface 561 to 559,
IPAddress 54 to 63 (of which +9 is a legitimate HSRP VIP cascade).

**This is not purely an ordering problem.** The Device failure is a
`ProtectedError` with *two surviving BGP peers* - children that were never
scheduled for deletion, so no reordering can succeed. The fix must decide, per
protecting relation, whether the child is in scope and should be deleted, or
the parent delete is genuinely not permitted and must be reported rather than
attempted.

The proven ordering pattern for the simpler case is
`_add_destination_fk_release_dependencies` in `bulk_merge.py`: batch-read
destination FK values, restore only the missing release edges, and let an
UPDATE that retains its reference still fail strictly. Generalising it needs
the same discipline. A partial, unverified generalisation was reverted before
the 2.6.2 gate; redo it against the validation harness with real data, not by
inspection.

#### Root cause found: the missing edge is an ordering gap, not a policy gap

`SquashMergeStrategy._build_fk_dependency_graph` step 3 builds its
child-before-parent DELETE edges out of each child's `prechange_data`:

```
# 3. Check DELETEs for dependencies on other DELETEs
for delete in deletes:
    if delete.prechange_data:
```

Branch-native bulk DELETE ObjectChanges can carry an **empty `prechange_data`**
when the row came from the direct first-baseline path - the identical gap
`_add_destination_fk_release_dependencies` already exists to close for UPDATEs.
The edge is therefore never created, the parent DELETE is attempted while a
child row still references it, and the database refuses it with `ProtectedError`.
The parent survives, and every later sync repeats the failure. That is how a
device kept two BGP peers and never converged.

`_add_protected_child_delete_dependencies` closes it the same way the UPDATE
case was closed: read the protecting references from the **destination**, which
needs no branch preimage, and restore the missing child-before-parent edges.

This is a genuine convergence fix rather than the suppression previously
contemplated. Suppression would have changed nothing - the existing fallback
already records a refused delete as a failure with accounting intact - whereas
the edge makes the delete actually succeed. It also needs no accounting change
at all, which removes requirement 2 below.

A reference whose row *survives* the run deliberately gets no edge, so a delete
the database genuinely must refuse still fails strictly instead of being
reordered into a false success.

**Cycle safety.** `_order_collapsed_changes_fast` raises on a cycle and aborts
the entire merge, and an edge added here once caused exactly that in the LAG
case. `_acyclic_delete_edges` therefore Kahn-sorts the candidate set first and
drops any edges touching a cycle, logging what it dropped. Mutually protecting
rows are undeletable by the database anyway, so dropping the edge only returns
that pair to the failure it would have had regardless - never to a lost merge.

Verified: removing the wiring makes
`test_child_delete_is_ordered_before_the_parent_it_protects` fail with
`AssertionError: 1 not less than 0`.

#### Earlier progress: detector landed, wiring deferred

`protecting_reference_blocked_deletes` in `bulk_merge.py` identifies deletes the
database will refuse: it walks each delete target's reverse PROTECT/RESTRICT
relations, batch-reads referencing rows, and reports only those whose
referencing row is *not* itself being deleted in the run. Seven tests, including
a real parent/child pair that asserts the relation is PROTECT before asserting
the block, and the converse case where deleting the child unblocks the parent.

Wiring it into the merge was attempted and **backed out**, because a full-suite
run surfaced two requirements the detector alone does not satisfy:

1. **In-run FK release must count as non-blocking.** The detector reads current
   destination state, so it flagged the case
   `test_dlm_protected_version_delete_follows_destination_fk_reassignment`
   covers: an UPDATE in the same run moves the reference away and the protected
   delete then succeeds. Suppressing it is a false positive. The check must
   consult `postchange_data` for the referencing field the way
   `_add_destination_fk_release_dependencies` already does, and treat a
   retained reference as blocking but a released one as clear.
2. **Suppressed deletes must be accounted.** Dropping entries from
   `to_process` broke the merge accounting invariant
   (`Bulk merge returned without accounting components`). A suppressed delete
   has to be recorded as a reported non-application, not silently removed.

Landing the detector without the wiring is deliberate: an unverified
generalisation of this exact behaviour was reverted before the 2.6.2 gate, and
repeating that is worse than an extra increment.

### 2. Delete-correctness comparator - ALREADY LANDED, dropped from scope

Measured against `~/forward-netbox-diffproof-backup-20260727`: 45 of its 55
touched files are byte-identical to main, and the residual lines exist only in
the backup because they are the *older* implementations that 2.6.2 to 2.6.4
replaced. `canonical_row_identity`, `canonical_row_identity_hash` and the
`sha256_canonical_row_identity_v1` scheme are present in
`workload_state.py` on main, which is byte-identical to the held copy.

Nothing to port. Item 1 can be trusted against the comparator already shipped.

### 3. Diff enablement - MEASURED, five of six already enabled

The blocked list above was stale. Enumerating every registry filename through
`_query_diff_ownership_mode` against `SUPPORTED_DIFF_OWNERSHIP_MODES`
(`global`, `device`, `cable_either_endpoint`, `contributor_relation`) gives
**45 of 48 entries diff-eligible today**:

| Query | Ownership mode | Status |
| --- | --- | --- |
| `forward_interfaces.nqe` | `device` | eligible |
| `forward_bgp_peers.nqe` | `device` | eligible |
| `forward_bgp_address_families.nqe` | `device` | eligible |
| `forward_bgp_peer_address_families.nqe` | `device` | eligible |
| `forward_ip_addresses_ipv4.nqe` | `contributor_relation` | eligible |
| `forward_devices*.nqe` | `feature_state_full_only` | blocked |
| `forward_aci_apic_cimc_inventory.nqe` | `unsafe_contributor_reduction` | blocked |

Interfaces - the 535,777-row win - entered Tier 2 with the ownership sets added
in #94, alongside the BGP trio and the Tier 3 IP-address reducer. Nothing to do.

The two that remain are **not blocked on item 1**. Each carries its own
correctness gate, recorded in `_query_diff_ownership_mode`:

- `forward_devices*.nqe`: device revisions combine modeled devices with endpoint
  candidates; exact collision and endpoint feature parity are not live-proven.
- `forward_aci_apic_cimc_inventory.nqe`: the parameterless draft exposes
  contributor selectors before a select-distinct reduction, so a changed
  contributor delta cannot prove an unchanged alternate controller preserves the
  final row.

Both gates need live proof, not a flag flip. Enabling either without that proof
would silently corrupt device or inventory data for every customer, so they stay
blocked and move to the 2.6.6 backlog.

### 4. Performance engines

Both proven and both held outside the repository, based on 2.6.1-era main
(`8707c27`) as uncommitted working trees:

- `~/forward-netbox-merge-backup-20260727` - set-based merge, 14.17x on MAC
  with full paired-branch equivalence including conflicts and concurrent main;
  also merge profiling and observability work. 50 files, +4,910/-637, 35
  untracked.
- `~/forward-netbox-copysql-backup-20260727` - COPY/SQL staging engine, 8.98x
  on MAC. 41 files, +3,986/-534, 14 untracked.
- `~/forward-netbox-diffproof-backup-20260727` - item 2 above. 55 files,
  +6,110/-405, 24 untracked.

The raw diff stats above are inflated: the backups sit four releases behind, so
main's later work counts as difference. Measured per line with `diff -u`, the
genuinely outstanding work is **about 1,411 lines for set-based merge and 719
for COPY/SQL**, not 15,000.

Both still touch the merge and apply-engine paths rewritten for 2.6.4, so each
engine is ported and validated independently. A bulk port risks silently
reverting the query-ID binding fix.

#### Outcome: set-based merge landed, COPY/SQL deferred

The "1,411 + 719 lines" estimate counted only the net diff of *tracked* files
and missed the new modules entirely. Actual outstanding work was ~3,239 lines
for set-based merge and ~2,488 for COPY/SQL.

Only the merge-related files were ported from each backup. The query-side files
in both trees (`query_registry.py`, `query_fetch_execution.py`, `views.py`,
`workload_state.py`, `forward_api_impl.py`, `query_binding*.py`) are the
2.6.2-era versions that main has since superseded; applying them would have
reverted the 2.6.4 query-ID binding fix. Verified after porting that
`_resolve_unpinned_customer_full_revision` and `resolve_nqe_query_head_commit`
are still present.

**Set-based merge: landed.** `merge_set_based.py`, `merge_observability.py`,
`merge_profiling.py`, the `ForwardMergeAttempt` model and migration 0045, the
`forward_profile_merge` command, and the benchmark/profiling scripts. Gated on
`enable_set_based_merge`, default off. All 10 of its own tests pass against the
current tree, including the paired-branch equivalence proof
(`test_paired_merge_matches_targets_side_tables_audit_lineage_and_conflicts`)
and `test_fault_injection_leaves_target_and_all_evidence_uncommitted`.

The port redesigned `_report_merge_progress` to take a `ForwardMergeAttempt` and
split `processed` into `merged_changes`/`failed_changes` with `model_progress`,
`started_at` and `force`. Main's two progress tests were updated to the new
contract rather than the contract being reverted to fit them.

**COPY/SQL: deferred to 2.6.6, not landed.** The engine ported cleanly, but 5 of
its 11 validation tests fail against NetBox 4.6.5 on a *fresh* database, for two
platform-drift reasons that predate this port:

1. `ValidationError: Cannot reassign MAC Address while it is designated as the
   primary MAC for an object` - a 4.6.5 rule the 4.6.3-era fixtures violate. The
   failure occurs in the **baseline** engine the test compares against, not in
   COPY/SQL.
2. `ForeignKeyViolation` on `extras_cachedvalue.object_type_id` from NetBox's
   search-cache backend across `TransactionTestCase` flushes. Adding
   `ContentType.objects.clear_cache()` did not resolve it.

Its validation harness needs genuine rework for 4.6.5, not a fixture tweak, and
weakening the tests to make them pass would defeat their purpose. Per this
plan's own constraint - *an engine that cannot be validated is not landed* - it
is backed out entirely rather than shipped flagged-off and unproven.

#### Bug found and fixed along the way: one malformed MAC failed the whole batch

The COPY/SQL suite surfaced a genuine latent defect in the shipping engine.
`bulk_orm_apply_macaddress` prefetched with `MACAddress.objects.filter(
mac_address__in=batch)`. Django prepares every element of that lookup through
NetBox's MAC field, so a single unparseable string raised `AddrFormatError`
while the *query* was being built - before any row was examined. One bad MAC
from Forward therefore failed every other MAC in the same shard.

`_is_parseable_mac` now filters the prefetch, and the per-row loop rejects an
unparseable value individually with `ForwardSyncDataError`, exactly as a missing
device/interface identity is already handled. Because the only coverage came
from the COPY/SQL suite that was backed out, two dedicated guards were added to
`test_bulk_adapter_parity.py`; removing the fix makes both fail with
`AddrFormatError`.

### 5. Smaller items

#### 360 no-op BGP-peer updates - FIXED

Root cause found. `bgp_peer_comments` rendered `session_state`,
`advertised_prefixes` and `received_prefixes` into the `comments` field.
Those come straight from `neighbor.sessionState` and `neighbor.statistics.*`
in `forward_bgp_peers.nqe` and move between snapshots on a healthy, entirely
unchanged session. Because `comments` participates in change detection
(`_model_field_value_matches`), every sync rewrote every peer: 360 UPDATEs,
360 ObjectChanges and 360 branch changes to stage and merge, on every run,
for peers whose configuration never changed.

The three volatile fields are no longer rendered. Stable descriptive state
(Router ID, peer type, peer device, peer VRF, peer router ID) is unchanged,
and `status` already carries active/offline. Forward remains authoritative
for the live counters. Owner decision, 2026-07-29.

Six guards in `test_bgp_peer_comment_churn.py`, including the exact churn case
(counters drift, comments identical) and the converse (a real configuration
change still changes the comments, so the guard has not made the field inert).

#### Shared-org platform publish - NOT DONE, premise was wrong

The backlog item read "publish the corrected null-safe platform query to the
shared `fwd` org, so other customers stop carrying the null-unsafe version."
Probing the live library read-only shows that premise does not hold:

- The shared repository is `fwd` (lowercase; `FWD` 404s), 2,109 queries.
- It contains exactly 7 NetBox queries, all under `/External/NetBox/`:
  Forward Device Models, Device Types, Device Vendors, Devices, Interfaces,
  Locations, and NetBox Devices.
- **None of the 7 reference `osVersion` at all.** There is no null-unsafe
  platform query there to correct, and no platform query there of any kind.

So the publish would not be a correction; it would be *adding a new query to
Forward Networks' own curated library*. That is a materially different act
from the one authorized, and our credential is org ADMIN on our own tenant -
scope for the ORG repository, not for `fwd`.

Not performed. Raise it with Forward directly if the query belongs in their
shared library.

#### IPAddress baseline shard degradation - diagnosed, deferred

70.2s to 149.0s per equal shard. The likely cause is in
`bulk_orm_apply_ipaddress`: the existing-IP prefetch builds an OR-chain of
functional lookups,

```python
host_filter = reduce(operator.or_, (Q(address__net_host=host) for host in batch))
```

`net_host` is `host(address)` on the `inet` column, so this is a chain of
per-value function calls that no plain index on `address` can serve. As the
table grows through a baseline load, each shard's scan gets more expensive -
which matches the observed shape of the degradation.

A single `HOST(address) = ANY(ARRAY[...])` would plan better, and a functional
index on `host(address)` would be the real fix, but that means adding an index
to a NetBox **core** table from a plugin migration.

Deferred deliberately: the same standard applied to COPY/SQL applies here.
The fix is unmeasured, no large IPAddress dataset is loaded to benchmark
against, and landing an unvalidated performance change to the IPAM apply path
is exactly what this plan forbids. Carried to 2.6.6 with the diagnosis above.

## Touched Surfaces

- `forward_netbox/utilities/bulk_merge.py` - protected child-before-parent
  DELETE ordering (`_protected_child_delete_edges`, `_acyclic_delete_edges`,
  `_add_protected_child_delete_dependencies`), the `ProtectedError` diagnostic,
  and the set-based merge profiling scopes.
- `forward_netbox/utilities/merge.py`, `ingestion_merge.py`,
  `single_branch_executor.py` - set-based merge selection and the
  `_report_merge_progress` observability contract.
- `forward_netbox/utilities/merge_set_based.py`, `merge_observability.py`,
  `merge_profiling.py` - new modules.
- `forward_netbox/models.py` + `migrations/0045_forwardmergeattempt.py` -
  `ForwardMergeAttempt`.
- `forward_netbox/management/commands/forward_profile_merge.py` - new command.
- `forward_netbox/utilities/apply_engine_bulk.py` - malformed-MAC isolation.
- `forward_netbox/utilities/sync_routing_impl.py` - BGP peer comment churn.
- Tests: `test_protecting_reference_deletes.py`, `test_set_based_merge.py`,
  `test_merge_observability.py`, `test_merge_profiling.py`,
  `test_bgp_peer_comment_churn.py`, `test_bulk_adapter_parity.py`,
  `test_ingestion_merge.py`.
- `README.md`, `CHANGELOG.md`, `.pre-commit-config.yaml`.

Not touched, deliberately: the query-side modules (`query_registry.py`,
`query_fetch_execution.py`, `views.py`, `workload_state.py`,
`forward_api_impl.py`, `query_binding*.py`). The held trees carry 2.6.2-era
versions of these; applying them would have reverted the 2.6.4 query-ID
binding fix.

## Approach

Dependency-first, and measure before porting rather than trusting the backlog.
Two of the five items turned out to be already landed or premised on something
untrue, and re-measuring caught both before any code was written.

Each engine is ported from its held tree file-by-file rather than as a bulk
patch, taking only the modules that belong to that engine, and validated with
the test suite that shipped alongside it. An engine whose validation suite does
not pass against the current platform is backed out rather than landed
flagged-off, because a flag that has never been proven is not a safety
mechanism.

Every behavioural fix lands with a guard that is checked by removing the fix
and confirming the guard fails.

## Rollback

Each item is a separate commit on `release/2.6.5` and reverts independently:

- Protected-delete ordering (`6731b9f`): revert restores the previous
  behaviour, where a protected parent delete is attempted first and fails with
  `ProtectedError`. No schema or data change.
- Set-based merge (`1ce4203`): the engine is gated on `enable_set_based_merge`
  and defaults off, so the deployed path is unchanged even without a revert.
  A full revert must also reverse migration `0045`
  (`manage.py migrate forward_netbox 0044`); `ForwardMergeAttempt` is
  observability only and holds no sync state, so dropping it loses merge
  progress history and nothing else.
- Malformed-MAC isolation (same commit): revert restores the prior behaviour
  where one unparseable MAC fails the whole shard.
- BGP comment churn (`1c0639b`): revert restores the volatile counters in
  `comments`, and with them the 360 writes per sync. The next sync rewrites
  every peer's comments either way, so no data is stranded in either
  direction.

No migration in this release is destructive, and none rewrites existing rows.

## Constraints

- Keep `netbox_branching` in the ingestion path.
- Audit NQE call volume on every sync-path change; Forward engineering has
  raised unnecessary query execution.
- No silent scope reduction: an engine that cannot be validated is not landed.

## Coverage lesson carried in

The 2.6.3 and 2.6.4 failures shipped because every test fixture fabricated a
repository listing row *containing* `lastCommitId`, which the live org listing
never returns. Mocks were more permissive than the API. Fixtures added here
must mirror observed live responses, and any new client interaction needs at
least one test whose fixture is copied from a real payload.

## Validation

Each item lands with its own gate run and its own tests. The release is
authorized only on the final tree.

## Release Authorization

- Evidence base commit: `8d9b8aab864c21acfc7516e9db6086dd90657f94`

- [x] `final-tree-full-gate` - `rtk env FORWARD_NETBOX_DOCKER_PROJECT=forward-netbox-release-gate FORWARD_NETBOX_POSTGRES_DATA_PATH=netbox-postgres-data FORWARD_NETBOX_WORKER_AUTORELOAD=0 NETBOX_VER=v4.6.5 invoke ci` passed on the final 2.6.5 tree with exit status 0: 220 harness tests OK, 66 scenario tests OK, 1687 Django tests OK with 3 skipped, the bulk-merge retry scale test OK, and the sync release gate reported clean strict release checks. The tree gated was byte-identical to the release commit's tree apart from two later Markdown-only edits backfilling the stale `docs/README.md` and `docs/01_User_Guide/README.md` compatibility tables, which had not been updated since v2.6.0. No Python, configuration or query source differs. The gate stages that read those files - lint, sensitive-content, and the CHANGELOG/README consistency check - were re-run on the final tree and passed.

  The evidence base commit above was rebound once after PR #102 squash-merged.
  Authorization binds the tagged commit to its parent, and a squash merge gives
  the release a different parent on `main` than it had on the branch, so the
  branch-side value (`7b41c5c`) could never match on `main`. This commit
  contains only that one-line rebind; the release content it authorizes is
  `8d9b8aa`, whose tree is the gated tree. Verified on `main` by
  `python scripts/verify_release_provenance.py`.
- [x] `exact-runtime-artifact` - `rtk env FORWARD_NETBOX_DOCKER_PROJECT=forward-netbox-release-gate FORWARD_NETBOX_POSTGRES_DATA_PATH=netbox-postgres-data FORWARD_NETBOX_WORKER_AUTORELOAD=0 NETBOX_VER=v4.6.5 invoke artifact-test` passed with exit status 0 against the installed `forward_netbox-2.6.5-py3-none-any.whl` on NetBox 4.6.5, Branching 1.1.1, Python 3.14, with 7 authenticated menu routes returning 200, migration 0045 applying cleanly with no drift, and a validated SBOM of 157 components.

The optional evidence ids are not recorded. This release is dominated by
convergence and churn corrections whose proof is in the gate, and its one
feature ships default-off behind `enable_set_based_merge` with its own
paired-branch equivalence and fault-injection tests passing. The release
owner's 2026-07-27 proportionality decision applies; see
`docs/03_Plans/completed/2026-07-27-release-authorization-proportionality.md`.

## Decision Log

- 2026-07-28: owner directed the full backlog plus the shipped fixes into
  2.6.5, treating 2.6.4 as burned.
- Correctness before performance: the perf ports are larger and more
  conflict-prone, so they land after the delete and diff work is verified.
