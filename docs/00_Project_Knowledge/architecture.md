# Architecture Summary

The canonical architecture map is `ARCHITECTURE.md` at the repository root so agents see it before entering the docs tree.

Use that file for production boundaries, stable large modules, future layer targets, and non-negotiable constraints. This docs page exists to keep MkDocs navigation strict while avoiding a second source of truth.
