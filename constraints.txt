# Pinned versions of the plugin's runtime dependencies as validated in CI — the
# "tested == shipped" set, so a range in pyproject.toml can't silently ship an
# untested resolution. Regenerate from the CI container with:
#   uv pip freeze | grep -iE \
#   '^(httpx|netboxlabs-netbox-branching|pyzipper|cryptography|netbox-(cisco-aci|dlm|peering-manager|routing))=='
# cryptography backs at-rest credential encryption (Fernet); it ships with NetBox
# but is pinned here for audit/reproducibility.
cryptography==50.0.0
httpx==0.28.1
netbox-cisco-aci==0.4.0
netbox-dlm==0.10.0
netboxlabs-netbox-branching==1.2.0
netbox-peering-manager==0.3.1
# netbox-routing 0.4.4 is upstream main (4.7 support, unreleased); the
# Dockerfile installs it from the git ref and this pin is the version that
# ref's metadata reports, so the constraint check still holds it to a value.
netbox-routing==0.4.4
netbox-validity==3.6.0
pyzipper==0.4.0
