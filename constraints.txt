# Pinned versions of the plugin's runtime dependencies as validated in CI — the
# "tested == shipped" set, so a range in pyproject.toml can't silently ship an
# untested resolution. Regenerate from the CI container with:
#   uv pip freeze | grep -iE \
#   '^(forward-sdk|httpx|netboxlabs-netbox-branching|pyzipper|cryptography|netbox-(cisco-aci|dlm|peering-manager|routing))=='
# cryptography backs at-rest credential encryption (Fernet); it ships with NetBox
# but is pinned here for audit/reproducibility.
cryptography==50.0.0
forward-sdk==0.1.16
httpx==0.28.1
netbox-cisco-aci==0.4.0
netbox-dlm==0.10.0
netboxlabs-netbox-branching==1.2.1
netbox-peering-manager==0.3.1
# netbox-routing is deliberately absent while it installs from upstream main:
# its 4.7 support (0.4.4) is merged but not tagged, so no PyPI version can be
# pinned here - and this file is fed to pip-audit, which resolves from PyPI
# and fails on a pin it cannot find. The Dockerfile installs the branch archive, and
# validated_runtime.py holds the installed version to exactly 0.4.4, failing
# closed if the archive ever reports something else. Restore the pin the moment
# upstream tags 0.4.4.
netbox-validity==3.6.0
pyzipper==0.4.0
